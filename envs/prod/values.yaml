replicaCount: 2

image:
  repository: jreb123/app-demo
  tag: "prod-latest"
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 80

resources:
  limits:
    cpu: 400m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

env:
  NAME: "Producción"
